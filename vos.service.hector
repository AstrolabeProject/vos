# Template file for Astrolabe VOS systemd service
[Unit]
# Docker must have successfully started *before* we launch VOS
Requires=docker.service
After=docker.service

[Service]
Type=oneshot
User=hickst
WorkingDirectory=/astrolabe/vos
ExecStart=/bin/docker compose -f docker-compose.yml -p vos up --detach
ExecStop=/bin/docker compose -p vos down
RemainAfterExit=yes

[Install]
# Start service when system boots
WantedBy=multi-user.target
